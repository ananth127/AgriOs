#This project is setuped with venv so first activate the venv by running the following command:
#venv\Scripts\activate
#pip install -r requirements.txt
#uvicorn app.main:app --reload

fastapi
uvicorn[standard]
gunicorn
sqlalchemy
psycopg2-binary
pydantic
email-validator
python-dotenv
python-jose[cryptography]
passlib
bcrypt==4.0.1
python-multipart
pillow
requests
pydantic-settings
GeoAlchemy2
shapely
# pandas # Removed to save memory
# scikit-learn # Removed to save memory
opentelemetry-api
opentelemetry-sdk
# ultralytics


# Voice Search - Free AI Alternatives
# Voice Search & AI
# openai-whisper # Removed: Causes OOM on 512MB instances (requires Torch)
# google-generativeai
# google-cloud-aiplatform
litellm

# Hugging Face Transformers for local AI inference
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0
huggingface_hub

# Audio processing without FFmpeg (production-ready)
soundfile>=0.12.0
librosa>=0.10.0
pydub>=0.25.0  # Optional: for MP3, OGG support
av>=10.0.0  # PyAV: for WebM/Opus support without system FFmpeg

google-cloud-speech
google-cloud-texttospeech

# Security - .env encryption
cryptography
